// get profile image from user, save to database, rename to the profilePictureId sent back, and store the profilePicture with new name in the profilePictures folder
// get post image from user, save the post to the pin on the database, rename to the postPictureId sent back, and store the post Picture with the new name in the postPictures folder
// grab default image for a pin with no posts on it
// grab default image for profile without a profile picture stored
// grab respective images for list of postPicrureIds/ paths to images?
// grab respective profile picture for the given profilePictureIds...